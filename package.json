{
  "name": "claude-bridge-mcp",
  "version": "1.0.0",
  "description": "MCP bridge for inter-Claude communication",
  "type": "module",
  "bin": {
    "claude-bridge-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --out-dir dist --clean",
    "dev": "tsx src/index.ts"
  },
  "keywords": ["mcp", "claude", "bridge", "multi-agent"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
